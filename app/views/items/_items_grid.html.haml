.features_items
  / features_items
  %h2.title.text-center
    = header_text
  - items.each do |item|
    .col-sm-3
      = render partial: 'items/item', locals: {item: item}
= paginate items
