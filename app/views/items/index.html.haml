.col-sm-9.padding-right
  .features_items
    / features_items
    %h2.title.text-center
      = category_heading_name(@category)
    - @items.each do |item|
      .col-sm-3
        = render partial: 'items/item', locals: {item: item}
  = paginate @items
